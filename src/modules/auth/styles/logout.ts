import { StyleSheet } from "react-native";

export const styles = () => {
  return StyleSheet.create({
    button: {
      marginTop: 50,
      width: 100,
      marginLeft: "auto",
      marginRight: "auto",
    }
  })
};