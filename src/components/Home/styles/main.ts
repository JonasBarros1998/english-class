import { StyleSheet } from "react-native";

export const styles = () => {
  return StyleSheet.create({
    title: {
      marginLeft: 10,
      marginTop: 10,
      marginBottom: 10,
    },
    fotter: {
      paddingBottom: 60
    }
  })
}