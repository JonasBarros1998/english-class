import {DefaultTheme} from 'react-native-paper';

export const theme = {
  ...DefaultTheme,
  dark: false,
  colors: {
    ...DefaultTheme.colors,
    primary: '#312e81',
    background: '#f2f2f2',
  },
};
